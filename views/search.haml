- if @divisions.empty?
  %h2 We couldn't find anything
  = "Sorry! We couldn't find any divisions lists when searching for <em>" +  @term + "</em>."
  %p You may find a division if you search again using a longer word.
- else
  %p= "You searched for <em>" + @term + "</em>. " + @divisions.length.to_s + " division lists contain that text:"
  %ol.search-results
    - @divisions.each do |division|
      - divnum = division["fields"]["number"]
      - divyear = division["fields"]["year"]
      %li
        %a{ :href => "/divisions/#{divyear}/#{divnum}" }= "Division #{divnum}, #{divyear}"
        
    